# Minimal default configuration. Model is resolved from config > ENV > default.
# Use `enchanter config --edit` to set provider/model/API key interactively,
# or `enchanter config --set model=... --set api_key=...` non-interactively.
config_version: 1
model:
  default: gpt-4.1-mini